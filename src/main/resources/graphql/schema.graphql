type Language {
    id: ID!
    name: String
    code: String
    color: String
    languageType: String
}

schema {
    query: Query
}

type Query {
    allLanguages: [Language]
    language(id: ID): Language
    languageType(languageType: String): [Language]
}